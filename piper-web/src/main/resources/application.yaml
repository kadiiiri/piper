spring:
  application:
    name: piper-web
  datasource:
    url: jdbc:h2:mem:piper-db
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop